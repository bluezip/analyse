'use strict'

analyse   = require './'
should    = require 'should'

describe 'Analyse report', ->

  describe 'image', ->

    it 'show data from json should not exist', ->

      (true).should.be.true;

